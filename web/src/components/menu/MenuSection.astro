---
import MenuItem from "./MenuItem.astro";

interface Props {
  category: {
    name: string;
    description: string;
    items: Array<{
      name: string;
      description: string;
      price: string;
      tags: string[];
    }>;
  };
}

const { category } = Astro.props;
const sectionId = category.name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
---

<section id={sectionId} data-menu-section class="scroll-mt-32 mb-14">
  <div class="mb-6">
    <h2 class="font-display text-2xl md:text-3xl text-brand-primary">
      {category.name}
    </h2>
    <p class="text-brand-text-muted mt-1">{category.description}</p>
    <div class="divider-amber w-20 mt-3"></div>
  </div>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
    {category.items.map((item) => (
      <MenuItem item={item} />
    ))}
  </div>
</section>
