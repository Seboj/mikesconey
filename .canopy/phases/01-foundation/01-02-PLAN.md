---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/site.ts
  - src/data/menu.json
autonomous: true
requirements: [FOUN-02, FOUN-03]

must_haves:
  truths:
    - "All restaurant facts (name, address, phone, hours) are defined in a single typed file"
    - "Changing site.ts updates every reference site-wide without touching other files"
    - "Menu data is structured JSON with categories and items, not hardcoded in templates"
    - "Menu JSON is populated with realistic Michigan coney island placeholder items"
  artifacts:
    - path: "src/data/site.ts"
      provides: "Typed restaurant configuration — single source of truth for all site-wide facts"
      exports: ["site", "SiteConfig"]
    - path: "src/data/menu.json"
      provides: "Structured menu data with categories and items"
      contains: "Coney Dogs"
  key_links:
    - from: "src/data/site.ts"
      to: "all components"
      via: "TypeScript import"
      pattern: "import.*site.*from.*data/site"
    - from: "src/data/menu.json"
      to: "menu page components"
      via: "JSON import"
      pattern: "import.*menu.*from.*data/menu"
---

<objective>
Create the typed data layer that serves as the single source of truth for all restaurant information and menu content.

Purpose: Establish the data contracts early so every component in later plans imports from these files rather than hardcoding any restaurant fact. This is the backbone of the "edit one file, update everywhere" pattern.
Output: src/data/site.ts with full TypeScript interface and populated config, src/data/menu.json with realistic coney island menu.
</objective>

<execution_context>
@/Users/kybernet/.claude/canopy/workflows/execute-plan.md
@/Users/kybernet/.claude/canopy/templates/summary.md
</execution_context>

<context>
@.canopy/STATE.md
@.canopy/ROADMAP.md
@.canopy/phases/01-foundation/01-CONTEXT.md
@.canopy/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed site configuration (site.ts)</name>
  <files>src/data/site.ts</files>
  <action>
    Create src/data/site.ts with a comprehensive TypeScript interface and populated configuration object.

    1. Define the SiteConfig interface with these sections:
       - name: string (restaurant name)
       - tagline: string
       - since: string (e.g., "Since 1995")
       - address: { street, city, state, zip, full } — full is the concatenated display string
       - phone: { display: string (formatted like "(248) 634-3555"), tel: string (tel:+12486343555) }
       - hours: Array<{ day: string, open: string, close: string, closed?: boolean }>
         - Typical diner hours: Mon-Fri 6AM-9PM, Sat 7AM-10PM, Sun 7AM-8PM
       - social: { instagram: string, facebook: string } — placeholder URLs
       - maps: { embedUrl: string, directionsUrl: string } — placeholder Google Maps URLs for Holly, MI
       - seo: { titleTemplate: string, description: string, ogImage: string }
         - titleTemplate: "%s | Mike's Coney Island"
         - description: "Classic coney dogs, burgers, and breakfast in Holly, Michigan. Family-owned since 1995."
         - ogImage: "/og-image.jpg" (placeholder path)

    2. Export both the interface and the populated config object:
       ```ts
       export interface SiteConfig { ... }
       export const site: SiteConfig = { ... };
       ```

    3. Add comments marking placeholder data:
       ```ts
       // PLACEHOLDER: Replace with actual restaurant details
       ```

    CRITICAL: Use realistic Holly, Michigan area data. The address should reference Holly, MI. Phone format should be a Michigan area code.
    CRITICAL: Every field that a component might need must be in this file. No restaurant fact should ever be hardcoded in a template.
  </action>
  <verify>
    <automated>cd /Users/kybernet/Projects/mikesconey && npx tsc --noEmit src/data/site.ts 2>&1 || npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/data/site.ts exports SiteConfig interface and site object
    - All fields populated with realistic placeholder data
    - TypeScript compiles without errors
    - Hours array covers all 7 days of the week
    - Phone has both display and tel: format
    - Address references Holly, Michigan
  </done>
</task>

<task type="auto">
  <name>Task 2: Create structured menu data (menu.json)</name>
  <files>src/data/menu.json</files>
  <action>
    Create src/data/menu.json with realistic Michigan coney island menu content.

    Schema structure:
    ```json
    {
      "categories": [
        {
          "name": "Category Name",
          "description": "Optional category description",
          "items": [
            {
              "name": "Item Name",
              "description": "Item description",
              "price": "$X.XX",
              "tags": ["optional", "dietary", "tags"]
            }
          ]
        }
      ]
    }
    ```

    Required categories with 4-8 items each (use realistic Michigan coney island pricing and items):

    1. **Coney Dogs** — Classic coneys, coney dog varieties, chili dogs
       - Items like: Regular Coney, Loose Burger (no bun), Coney Island Special, Chili Dog, Cheese Coney
       - Price range: $2.99-$5.99

    2. **Burgers** — Classic diner burgers
       - Items like: Hamburger, Cheeseburger, Bacon Cheeseburger, Mushroom Swiss, Patty Melt
       - Price range: $5.99-$9.99

    3. **Sandwiches** — Diner sandwich classics
       - Items like: BLT, Grilled Cheese, Club, Tuna Melt, Philly Steak
       - Price range: $5.99-$8.99

    4. **Breakfast** — All-day breakfast (classic diner style)
       - Items like: Two Eggs Any Style, Omelettes (Western, Cheese, Veggie), Pancakes, French Toast, Breakfast Burrito
       - Price range: $4.99-$9.99

    5. **Sides** — Fries, onion rings, etc.
       - Items like: French Fries, Onion Rings, Chili Cheese Fries, Cole Slaw, Side Salad
       - Price range: $2.49-$4.99

    6. **Beverages** — Drinks
       - Items like: Coffee, Soft Drinks, Milkshakes, Orange Juice, Hot Chocolate
       - Price range: $1.99-$4.99

    CRITICAL: Prices as strings (e.g., "$4.99") — display-only, no math needed.
    CRITICAL: Descriptions should be appetizing and specific, not generic. Reference Michigan/coney island culture where appropriate.
    CRITICAL: Include tags array (can be empty) for future dietary callout support (GF, V, Spicy).
  </action>
  <verify>
    <automated>node -e "const m = require('/Users/kybernet/Projects/mikesconey/src/data/menu.json'); console.log('Categories:', m.categories.length); m.categories.forEach(c => console.log(c.name + ':', c.items.length, 'items'));"</automated>
  </verify>
  <done>
    - menu.json has 6 categories with 4-8 items each
    - Every item has name, description, price, and tags fields
    - Prices are strings in "$X.XX" format
    - Content is realistic Michigan coney island fare
    - JSON is valid and parseable
  </done>
</task>

</tasks>

<verification>
1. `node -e "const s = require('./src/data/site.ts'); ..."` — TypeScript file exports correctly (or verify via astro build)
2. `node -e "JSON.parse(require('fs').readFileSync('src/data/menu.json'))"` — Valid JSON
3. menu.json has at least 6 categories and 30+ total items
4. site.ts has all required fields: name, address, phone, hours, social, maps, seo
</verification>

<success_criteria>
- site.ts is the single source of truth for all restaurant facts
- menu.json contains realistic, appetizing menu content
- Both files have clear TypeScript types and are importable by any Astro component
- No restaurant fact exists outside these data files
</success_criteria>

<output>
After completion, create `.canopy/phases/01-foundation/01-02-SUMMARY.md`
</output>
