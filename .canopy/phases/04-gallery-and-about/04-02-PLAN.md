---
phase: 04-gallery-and-about
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/about.astro
  - src/components/about/AboutHero.astro
  - src/components/about/BrandStory.astro
  - src/components/about/HighlightCards.astro
  - src/components/about/ReviewsCallout.astro
  - src/data/reviews.ts
autonomous: true
requirements: [ABUT-01, ABUT-02]
must_haves:
  truths:
    - "The About page tells a brand story specific to Mike's Coney Island and Holly, MI — not generic restaurant copy"
    - "The About page displays 3-5 highlighted customer reviews with star ratings and reviewer names"
    - "The About page has an overall Google rating badge showing the rating prominently"
    - "The reviews on the About page are DIFFERENT from the homepage SocialProof reviews"
    - "The About page uses warm accent backgrounds and feels visually distinct from the homepage"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "Full About page composition"
      min_lines: 15
    - path: "src/components/about/BrandStory.astro"
      provides: "Brand narrative mentioning Holly MI and Mike's history"
      min_lines: 20
    - path: "src/components/about/ReviewsCallout.astro"
      provides: "Static Google reviews section with star ratings"
      min_lines: 30
    - path: "src/data/reviews.ts"
      provides: "About page review data separate from homepage"
      exports: ["aboutReviews", "aboutOverallRating", "aboutReviewCount"]
  key_links:
    - from: "src/pages/about.astro"
      to: "src/components/about/ReviewsCallout.astro"
      via: "Astro component import"
      pattern: "import.*ReviewsCallout"
    - from: "src/components/about/ReviewsCallout.astro"
      to: "src/data/reviews.ts"
      via: "data import"
      pattern: "import.*aboutReviews.*from.*reviews"
    - from: "src/components/about/BrandStory.astro"
      to: "src/data/site.ts"
      via: "site data import for restaurant name and since year"
      pattern: "import.*site.*from.*site"
---

<objective>
Build the complete About page with brand story, highlight cards, and Google reviews callout.

Purpose: Visitors read a warm, personal brand story that makes Mike's Coney Island feel like a local neighborhood spot they need to visit. The page builds credibility with highlighted customer reviews and communicates the restaurant's identity through its Holly, MI community roots.

Output: Working About page at /about/ with hero, brand narrative, highlight cards, and reviews section.
</objective>

<execution_context>
@/Users/kybernet/.claude/canopy/workflows/execute-plan.md
@/Users/kybernet/.claude/canopy/templates/summary.md
</execution_context>

<context>
@.canopy/STATE.md
@.canopy/ROADMAP.md

<interfaces>
<!-- Existing site data -->
From src/data/site.ts:
```typescript
export const site: SiteConfig = {
  name: "Mike's Coney Island",
  tagline: "Classic Coneys, Burgers & Breakfast in Holly, Michigan",
  since: "Since 1995",
  // ...
};
```

<!-- Existing SocialProof component pattern (reference, do NOT reuse) -->
From src/components/SocialProof.astro:
```typescript
// Homepage reviews — 3 reviews, overallRating 4.8, reviewCount 127
const reviews = [
  { text: "Best coney dogs in Oakland County...", author: "Sarah M.", rating: 5 },
  { text: "My family has been coming here...", author: "David R.", rating: 5 },
  { text: "Great local spot with fair prices...", author: "Jennifer L.", rating: 5 },
];
```
IMPORTANT: About page must use DIFFERENT reviews from different reviewers.

<!-- Existing star SVG pattern from SocialProof -->
Star SVG markup (reuse this exact pattern):
```html
<svg class="w-5 h-5 text-brand-secondary" viewBox="0 0 20 20" fill="currentColor">
  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
</svg>
```

<!-- Existing brand tokens -->
Brand colors (from global.css):
- --color-brand-primary: #B91C1C (red)
- --color-brand-secondary: #CA8A04 (gold)
- --color-brand-accent: #FEF3C7 (warm cream)
- --color-brand-dark: #1E293B
- --color-brand-light: #FFFBEB
- .bg-texture-warm: subtle dot texture pattern

<!-- Existing About page skeleton (to be completely replaced) -->
From src/pages/about.astro:
```astro
<BaseLayout title="About">
  <!-- Currently has 2 basic paragraphs of placeholder text -->
</BaseLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About page data and components</name>
  <files>
    src/data/reviews.ts
    src/components/about/AboutHero.astro
    src/components/about/BrandStory.astro
    src/components/about/HighlightCards.astro
    src/components/about/ReviewsCallout.astro
  </files>
  <action>
    1. Create `src/data/reviews.ts` with About-specific review data:
       ```typescript
       export interface Review {
         text: string;
         author: string;
         rating: number;
       }

       export const aboutReviews: Review[] = [/* 5 reviews */];
       export const aboutOverallRating = 4.8;
       export const aboutReviewCount = 127;
       ```
       Include 5 reviews with DIFFERENT names and quotes than the homepage SocialProof (Sarah M., David R., Jennifer L. are taken). Use names like: "Mike T.", "Karen W.", "Brian S.", "Lisa P.", "Tom H.". Reviews should feel authentic — mention specific menu items, the atmosphere, the staff. Mix of 4-star and 5-star ratings for realism.

    2. Create `src/components/about/AboutHero.astro`:
       - Full-width warm background: `bg-brand-accent bg-texture-warm`
       - Padding: `py-16 md:py-24 px-4 md:px-8`
       - Centered content (max-w-3xl mx-auto text-center):
         - Small accent label: "OUR STORY" in uppercase, text-sm, font-bold, tracking-widest, text-brand-primary
         - h1: "Welcome to Mike's Coney Island" with `font-heading text-4xl md:text-5xl font-bold text-brand-text-heading mt-4 mb-4`
         - Subtitle: "Serving Holly, Michigan since 1995" with `text-lg text-brand-text-muted`
         - Use `site.since` and `site.name` from site.ts imports for the dynamic values

    3. Create `src/components/about/BrandStory.astro`:
       - Section: `py-16 md:py-20 px-4 md:px-8`
       - Container: `mx-auto max-w-3xl`
       - Two-part layout:
         a) Brand narrative (2-3 paragraphs). This MUST feel personal and specific to Mike's Coney Island in Holly, MI. Write in a warm, conversational first-person-plural tone ("We've been..."). Content should:
            - Reference Holly, MI by name multiple times
            - Mention the tradition of Michigan coney islands
            - Reference specific menu highlights (coneys, breakfast, burgers)
            - Feel like the owner is talking directly to you
            - Use the "Since 1995" from site.ts
            - NOT sound generic — avoid phrases like "We pride ourselves" or "committed to excellence"
            - Example opening: "Walk through our doors on any given morning and you'll hear the sizzle of eggs on the griddle, the friendly shout of 'order up!', and the kind of easy conversation that only happens in a place where everybody's a regular."
         b) Restaurant placeholder photo area:
            - Branded placeholder div (similar to gallery placeholders) with text "Restaurant photo coming soon"
            - Styled as a warm-toned card with rounded corners, aspect-[16/9], centered icon and text
            - `mt-10 rounded-xl overflow-hidden`

       - Use prose-like typography: text-lg, leading-relaxed, text-brand-text-body
       - Paragraphs separated with `mt-6`

    4. Create `src/components/about/HighlightCards.astro`:
       - Section with light brand background: `py-16 md:py-20 px-4 md:px-8 bg-brand-light`
       - Section header with accent bar pattern (match SocialProof heading style):
         - `<div class="w-12 h-1 bg-brand-primary mx-auto mb-4 rounded-full"></div>`
         - h2: "What Brings People Back" centered, font-heading, text-2xl md:text-3xl, font-bold
       - 3 cards in a responsive grid: `grid md:grid-cols-3 gap-6 max-w-4xl mx-auto mt-10`
       - Each card:
         - White background, rounded-xl, p-6, shadow-sm, border border-brand-divider/50
         - Icon at top (inline SVG, brand-primary color, w-10 h-10)
         - Title (font-heading, font-bold, text-lg, mt-4)
         - Short description (text-brand-text-body, text-sm, mt-2)
       - Three cards:
         1. "Classic Coneys" — icon: hot dog/fork — "Our coney dogs are smothered in homemade chili sauce, diced onions, and yellow mustard. It's the recipe that put us on the map."
         2. "Homestyle Breakfast" — icon: sunrise/plate — "From fluffy pancakes to build-your-own omelets, breakfast at Mike's is the real deal. Served all day, because good mornings shouldn't have a deadline."
         3. "Friendly Faces" — icon: heart/smile — "Walk in a stranger, leave as a regular. Our crew knows your name, your order, and probably your kids' names too."

    5. Create `src/components/about/ReviewsCallout.astro`:
       - Import `aboutReviews`, `aboutOverallRating`, `aboutReviewCount` from `../../data/reviews`
       - Section: `py-16 md:py-20 px-4 md:px-8 bg-brand-accent/40 bg-texture-warm`
       - Container: `mx-auto max-w-5xl`
       - Section header:
         - Accent bar + "What Our Customers Say" h2 (same pattern as SocialProof)
         - Below heading: prominent Google rating badge:
           - Flex row centered: Google "G" icon (or text "Google") + 5 star SVGs + "4.8" bold + "(127 reviews)" muted
       - Reviews in a grid: different from homepage — use `grid md:grid-cols-2 lg:grid-cols-3 gap-6` for 5 reviews (2+2+1 on desktop fills nicely, or 3+2)
       - Each review card (same white card style as SocialProof but distinct):
         - Large opening quotation mark: `text-4xl text-brand-primary/20 font-serif leading-none` showing a stylized open-quote
         - Quote text: `text-brand-text-body text-sm md:text-base leading-relaxed`
         - Star rating row (reuse the SVG star pattern from SocialProof)
         - Author name: `text-brand-text-muted text-sm font-medium`
       - Bottom: "See all reviews on Google" link (muted, centered)

    IMPORTANT: Review data must use DIFFERENT reviewer names and quotes than homepage SocialProof.astro (which uses Sarah M., David R., Jennifer L.).
  </action>
  <verify>
    <automated>ls src/components/about/*.astro | wc -l && ls src/data/reviews.ts && grep -c "aboutReviews" src/data/reviews.ts && grep -c "Sarah M.\|David R.\|Jennifer L." src/data/reviews.ts || echo "No homepage reviewer names found in About reviews"</automated>
  </verify>
  <done>4 About components created (AboutHero, BrandStory, HighlightCards, ReviewsCallout), reviews.ts has 5 unique reviews with different names than homepage, all components follow brand styling patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Compose About page and verify build</name>
  <files>
    src/pages/about.astro
  </files>
  <action>
    1. Completely rewrite `src/pages/about.astro`:
       ```astro
       ---
       import BaseLayout from "../layouts/BaseLayout.astro";
       import AboutHero from "../components/about/AboutHero.astro";
       import BrandStory from "../components/about/BrandStory.astro";
       import HighlightCards from "../components/about/HighlightCards.astro";
       import ReviewsCallout from "../components/about/ReviewsCallout.astro";
       ---

       <BaseLayout title="About" description="Learn about Mike's Coney Island — serving classic coneys, burgers, and breakfast to the Holly, Michigan community since 1995.">
         <AboutHero />
         <BrandStory />
         <HighlightCards />
         <ReviewsCallout />
       </BaseLayout>
       ```

    2. Run `npx astro build` to verify the full page compiles without errors.

    3. Verify the brand narrative in BrandStory.astro mentions "Holly" at least twice and "Mike's" or the restaurant name at least once. If not, fix the copy.

    4. Verify ReviewsCallout uses different reviewer names than SocialProof.astro. Check by comparing the author fields.

    Page section flow (top to bottom):
    - AboutHero: warm accent background, "OUR STORY" label, large heading, subtitle
    - BrandStory: white background, 2-3 paragraphs of personal narrative, restaurant photo placeholder
    - HighlightCards: light background, 3 feature cards
    - ReviewsCallout: warm textured background, Google rating badge, 5 review cards
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>About page builds successfully at /about/ with all 4 sections. Brand story mentions Holly MI and feels personal. Reviews section shows 5 different reviews from homepage. Visual flow alternates between warm and white backgrounds.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. About page exists at /about/
3. Brand narrative mentions "Holly" and "Mike's" — not generic restaurant copy
4. About page uses site.ts data (restaurant name, since year)
5. 5 reviews displayed with star ratings and reviewer names
6. Review names do NOT overlap with homepage SocialProof (Sarah M., David R., Jennifer L.)
7. Overall Google rating badge displays prominently
8. Page sections alternate backgrounds (warm → white → light → warm)
</verification>

<success_criteria>
- About page builds and renders with all 4 sections
- Brand narrative is warm, personal, and specific to Mike's Coney Island in Holly, MI
- Reviews section shows 5 unique reviews with star ratings
- No overlap between About page reviews and homepage SocialProof reviews
- Visual style matches the brand palette and typography established in Phases 1-2
</success_criteria>

<output>
After completion, create `.canopy/phases/04-gallery-and-about/04-02-SUMMARY.md`
</output>
