---
phase: 04-gallery-and-about
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/gallery.ts
  - src/pages/gallery.astro
  - src/components/gallery/GalleryGrid.astro
  - src/components/gallery/GalleryPlaceholder.astro
  - public/images/gallery/placeholder-food-1.svg
  - public/images/gallery/placeholder-food-2.svg
  - public/images/gallery/placeholder-food-3.svg
  - public/images/gallery/placeholder-food-4.svg
  - public/images/gallery/placeholder-atmosphere-1.svg
  - public/images/gallery/placeholder-atmosphere-2.svg
  - public/images/gallery/placeholder-atmosphere-3.svg
  - public/images/gallery/placeholder-people-1.svg
  - public/images/gallery/placeholder-people-2.svg
autonomous: true
requirements: [GALL-01, GALL-02, GALL-03]
must_haves:
  truths:
    - "The gallery page displays a responsive grid of images — 3 columns on desktop, 2 on tablet, 1 on mobile"
    - "All 9 gallery cells render branded SVG placeholders with warm colors and 'Photo coming soon' text"
    - "No external image URLs exist anywhere in the gallery (no picsum.photos, placehold.co, unsplash)"
    - "Each gallery cell maintains a consistent 4:3 aspect ratio with no layout shift"
    - "Gallery images above the fold load eagerly; below-fold images use lazy loading"
  artifacts:
    - path: "src/data/gallery.ts"
      provides: "Gallery image data array with typed interface"
      exports: ["GalleryImage", "galleryImages"]
    - path: "src/components/gallery/GalleryGrid.astro"
      provides: "Responsive CSS Grid gallery layout"
      min_lines: 20
    - path: "public/images/gallery/placeholder-food-1.svg"
      provides: "Branded SVG placeholder for food category"
  key_links:
    - from: "src/pages/gallery.astro"
      to: "src/components/gallery/GalleryGrid.astro"
      via: "Astro component import"
      pattern: "import.*GalleryGrid"
    - from: "src/components/gallery/GalleryGrid.astro"
      to: "src/data/gallery.ts"
      via: "data import"
      pattern: "import.*galleryImages.*from.*gallery"
---

<objective>
Build the gallery page with a responsive image grid and branded SVG placeholders.

Purpose: Customers can browse a visually appealing gallery of food and atmosphere photos (placeholders for now) that looks intentional and on-brand rather than broken or empty. This is the core gallery layout that the lightbox (Plan 03) will attach to.

Output: Working gallery page at /gallery/ with 9 branded placeholder images in a responsive grid.
</objective>

<execution_context>
@/Users/kybernet/.claude/canopy/workflows/execute-plan.md
@/Users/kybernet/.claude/canopy/templates/summary.md
</execution_context>

<context>
@.canopy/STATE.md
@.canopy/ROADMAP.md

<interfaces>
<!-- Existing site data pattern from src/data/site.ts -->
From src/data/site.ts:
```typescript
export interface SiteConfig { ... }
export const site: SiteConfig = { ... };
```

<!-- Existing brand tokens from src/styles/global.css -->
Brand colors:
- --color-brand-primary: #B91C1C (red)
- --color-brand-primary-light: #DC2626
- --color-brand-secondary: #CA8A04 (gold)
- --color-brand-accent: #FEF3C7 (warm cream)
- --color-brand-dark: #1E293B
- --color-brand-light: #FFFBEB
- --color-brand-text-heading: #0F172A
- --color-brand-text-body: #334155
- --color-brand-text-muted: #64748B
- --color-brand-divider: #E2E8F0

<!-- Existing gallery page skeleton (to be replaced) -->
From src/pages/gallery.astro:
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Gallery">
  <!-- Currently shows 6 inline SVG placeholders in a basic grid -->
</BaseLayout>
```

<!-- Existing BaseLayout pattern -->
From src/layouts/BaseLayout.astro:
```astro
<BaseLayout title="Gallery">
  <slot />  <!-- Page content goes here -->
</BaseLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gallery data layer and SVG placeholders</name>
  <files>
    src/data/gallery.ts
    public/images/gallery/placeholder-food-1.svg
    public/images/gallery/placeholder-food-2.svg
    public/images/gallery/placeholder-food-3.svg
    public/images/gallery/placeholder-food-4.svg
    public/images/gallery/placeholder-atmosphere-1.svg
    public/images/gallery/placeholder-atmosphere-2.svg
    public/images/gallery/placeholder-atmosphere-3.svg
    public/images/gallery/placeholder-people-1.svg
    public/images/gallery/placeholder-people-2.svg
  </files>
  <action>
    1. Create `src/data/gallery.ts` with a typed interface and data array:
       ```typescript
       export interface GalleryImage {
         id: string;
         src: string;
         alt: string;
         caption?: string;
         category: 'food' | 'atmosphere' | 'people';
         isPlaceholder: boolean;
       }
       ```
       Populate `galleryImages` with 9 entries (4 food, 3 atmosphere, 2 people). Each points to its SVG in `/images/gallery/`. Give each entry a descriptive alt text that reflects what the real photo would show (e.g., "Classic coney dogs with homemade chili sauce", "Cozy diner counter with red stools", "Friendly staff behind the counter"). Include captions on food items.

    2. Create `public/images/gallery/` directory.

    3. Create 9 SVG placeholder files. Each SVG:
       - viewBox="0 0 400 300" (4:3 aspect ratio)
       - Warm background using brand-accent (#FEF3C7) with rounded corners (rx="8")
       - Subtle texture overlay using brand-secondary (#CA8A04) at 8% opacity
       - Category-appropriate icon centered in the image:
         - Food items: fork-and-knife icon (simple path)
         - Atmosphere items: building/storefront icon
         - People items: person/group icon
       - Icon stroke color: brand-primary (#B91C1C) at 30% opacity
       - Text "Photo coming soon" below the icon in brand-primary at 50% opacity, 14px sans-serif
       - Each SVG should be slightly different (vary icon positions or add small decorative elements) so the grid doesn't look like carbon copies

    IMPORTANT: No external URLs. All image sources must be local paths starting with /images/gallery/.
  </action>
  <verify>
    <automated>ls public/images/gallery/*.svg | wc -l && grep -c "isPlaceholder: true" src/data/gallery.ts && grep -rn "picsum\|placehold\.co\|unsplash" src/data/gallery.ts public/images/gallery/ || echo "No external URLs found"</automated>
  </verify>
  <done>9 SVG files exist in public/images/gallery/, gallery.ts exports galleryImages array with 9 typed entries, all sources are local paths, no external image URLs anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Build responsive gallery grid and update gallery page</name>
  <files>
    src/components/gallery/GalleryGrid.astro
    src/pages/gallery.astro
  </files>
  <action>
    1. Create `src/components/gallery/GalleryGrid.astro`:
       - Import `galleryImages` from `../../data/gallery`
       - Render a CSS Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
       - Each grid cell is a `<button>` element (clickable for future lightbox) with:
         - `type="button"` for accessibility
         - `class="group aspect-[4/3] overflow-hidden rounded-lg cursor-pointer relative focus:outline-none focus:ring-2 focus:ring-brand-primary focus:ring-offset-2"`
         - `data-gallery-item` attribute
         - `data-index={index}` for lightbox navigation
         - `data-src={image.src}` for lightbox image source
         - `data-alt={image.alt}` for lightbox alt text
         - `data-caption={image.caption || ''}` for lightbox caption
         - `aria-label={\`View \${image.alt}\`}`
       - Inside each button: `<img>` tag with:
         - `src={image.src}`, `alt={image.alt}`
         - `class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"`
         - `loading={index < 6 ? 'eager' : 'lazy'}` (first 6 eager, rest lazy)
         - `width="400"` `height="300"` for CLS prevention
       - If image has caption: overlay gradient at bottom with caption text
         - `<div class="absolute bottom-0 inset-x-0 bg-gradient-to-t from-black/60 to-transparent p-3 opacity-0 group-hover:opacity-100 transition-opacity">`
         - `<p class="text-sm text-white">{image.caption}</p>`

    2. Rewrite `src/pages/gallery.astro` completely:
       - Import BaseLayout and GalleryGrid
       - Page structure:
         - Section with `py-16 md:py-20 px-4 md:px-8` padding
         - Centered heading area (max-w-5xl mx-auto):
           - Accent bar: `<div class="w-12 h-1 bg-brand-primary mx-auto mb-4 rounded-full"></div>`
           - h1: "Gallery" with `font-heading text-3xl md:text-4xl font-bold text-brand-text-heading text-center mb-3`
           - Subtitle: "A peek inside Mike's Coney Island" with `text-center text-brand-text-muted mb-12`
         - GalleryGrid component (inside max-w-6xl mx-auto container for wider grid)
         - Bottom note: centered text "Real photos coming soon! Stay tuned." in muted text with mt-12

    Match the visual language of the existing homepage sections (accent bar above headings from SocialProof.astro pattern).
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>Gallery page renders at /gallery/ with a responsive 3/2/1 column grid of 9 placeholder images. Each cell has 4:3 aspect ratio, hover scale effect, and proper loading attributes. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Gallery page exists at /gallery/
3. Grid displays 3 columns on desktop (>1024px), 2 on tablet (>640px), 1 on mobile
4. All 9 images are branded SVG placeholders with warm brand colors
5. No external image URLs in codebase (grep for picsum, placehold.co, unsplash returns nothing)
6. Each grid cell maintains 4:3 aspect ratio
7. First 6 images have loading="eager", last 3 have loading="lazy"
</verification>

<success_criteria>
- Gallery page builds and renders with 9 placeholder images in responsive grid
- Data layer (gallery.ts) is typed and easily updatable
- SVG placeholders look intentional and branded, not broken
- Grid cells have data attributes ready for lightbox integration (Plan 03)
- Zero layout shift (CLS) due to fixed aspect ratios
</success_criteria>

<output>
After completion, create `.canopy/phases/04-gallery-and-about/04-01-SUMMARY.md`
</output>
