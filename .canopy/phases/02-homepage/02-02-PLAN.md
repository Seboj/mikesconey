---
phase: 02-homepage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/AboutTeaser.astro, src/components/SocialProof.astro]
autonomous: true
requirements: [HOME-04, DSGN-02]
must_haves:
  truths:
    - "Homepage has an about teaser with 2-3 personality-driven sentences about Mike's Coney Island"
    - "About teaser includes a Read Our Story link to /about/"
    - "Social proof section displays 3 placeholder customer quotes with star ratings"
    - "Visual design uses brand colors, warm backgrounds, and generous whitespace — not generic template look"
  artifacts:
    - path: "src/components/AboutTeaser.astro"
      provides: "About teaser section with personality copy and Read Our Story link"
      min_lines: 25
    - path: "src/components/SocialProof.astro"
      provides: "Social proof section with 3 customer quotes and star rating"
      min_lines: 40
  key_links:
    - from: "src/components/AboutTeaser.astro"
      to: "/about/"
      via: "anchor href"
      pattern: "href=.*about"
    - from: "src/components/SocialProof.astro"
      to: "star rating display"
      via: "SVG stars"
      pattern: "svg.*viewBox"
---

<objective>
Build the about teaser and social proof sections — the two content sections that give Mike's personality and credibility below the fold.

Purpose: HOME-04 (about teaser with link to About page), DSGN-02 (visually striking design with warm textures and brand identity)
Output: AboutTeaser.astro, SocialProof.astro components
</objective>

<execution_context>
@/Users/kybernet/.claude/canopy/workflows/execute-plan.md
@/Users/kybernet/.claude/canopy/templates/summary.md
</execution_context>

<context>
@.canopy/STATE.md
@.canopy/ROADMAP.md
@.canopy/phases/02-homepage/02-RESEARCH.md

<interfaces>
From src/data/site.ts:
```typescript
export interface SiteConfig {
  name: string;
  tagline: string;
  since: string;
  // ... (full interface available in site.ts)
}
export const site: SiteConfig;
```

From src/styles/global.css — brand colors available as Tailwind utilities:
- brand-primary (#B91C1C), brand-secondary (#CA8A04)
- brand-dark (#1E293B), brand-dark-deeper (#0F172A)
- brand-light (#FFFBEB), brand-accent (#FEF3C7)
- brand-text-heading, brand-text-body, brand-text-muted, brand-text-light
- brand-divider (#E2E8F0)
- bg-texture-warm (subtle dot pattern — added by Plan 01)

Font families: font-heading (Outfit), font-body (Inter)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutTeaser component</name>
  <files>src/components/AboutTeaser.astro</files>
  <action>
Create `src/components/AboutTeaser.astro`:

1. Import `{ site }` from `../data/site`.

2. Build the about teaser section:
   - Outer section: `py-16 md:py-20 px-4 md:px-8 bg-white`
   - Inner container: `mx-auto max-w-4xl`
   - Use a two-column layout on desktop: `grid md:grid-cols-5 gap-8 md:gap-12 items-center`
     - Left column (md:col-span-3): text content
     - Right column (md:col-span-2): decorative element

3. Text content (left column):
   - Accent bar: `w-12 h-1 bg-brand-primary rounded-full mb-4`
   - Section heading: "A Holly Tradition" (or "Our Story") — font-heading, text-2xl md:text-3xl, font-bold, text-brand-text-heading, mb-6
   - About paragraph 1: personality-driven, warm tone. Example:
     "Mike's Coney Island has been a Holly, Michigan staple since 1995. From our signature coney dogs loaded with our homemade chili to fluffy breakfast plates that get you going — we're the kind of place where regulars wave from their booth and newcomers feel right at home."
   - About paragraph 2 (shorter):
     "Stop in, grab a seat at the counter, and see what keeps the neighborhood coming back."
   - Style paragraphs: `text-brand-text-body text-base md:text-lg leading-relaxed mb-4`
   - "Read Our Story" link: `inline-flex items-center gap-2 text-brand-primary font-medium hover:text-brand-primary-light transition-colors mt-2`
   - Add a right-arrow SVG icon (w-4 h-4) after the link text — simple chevron-right path

4. Decorative element (right column):
   - A warm-toned decorative block suggesting the restaurant vibe
   - `bg-brand-accent rounded-2xl p-8 md:p-10` with the `bg-texture-warm` class
   - Inside: center a large quotation or decorative text:
     - Large quotation mark SVG in brand-primary at 20% opacity, w-16 h-16
     - Below it: `site.since` in font-heading, text-brand-secondary, text-lg font-bold
     - Below that: `site.address.city + ", " + site.address.state` in text-brand-text-muted, text-sm

CRITICAL constraints:
- Import `site.since` and `site.address` from site.ts — do NOT hardcode "1995", "Holly", or "Michigan"
- The copy CAN be hardcoded (it's editorial content, not data), but reference site.name where the restaurant name appears
- Link MUST point to `/about/`
  </action>
  <verify>
    <automated>cd /Users/kybernet/Projects/mikesconey && test -f src/components/AboutTeaser.astro && grep -q 'href=.*/about/' src/components/AboutTeaser.astro && grep -q "site" src/components/AboutTeaser.astro && echo "PASS: AboutTeaser complete" || echo "FAIL"</automated>
  </verify>
  <done>AboutTeaser.astro exists with warm personality-driven copy, Read Our Story link to /about/, decorative accent block using brand colors, and site.ts data for restaurant name and location.</done>
</task>

<task type="auto">
  <name>Task 2: Create SocialProof component with placeholder reviews and star rating</name>
  <files>src/components/SocialProof.astro</files>
  <action>
Create `src/components/SocialProof.astro`:

1. Define placeholder review data in the frontmatter (this is temporary content until real reviews are collected):
```typescript
const reviews = [
  {
    text: "Best coney dogs in Oakland County, hands down. The chili is homemade and you can tell. We drive 20 minutes just for their breakfast.",
    author: "Sarah M.",
    rating: 5,
  },
  {
    text: "My family has been coming here for years. The staff knows our names and the food is always consistent. Holly wouldn't be the same without Mike's.",
    author: "David R.",
    rating: 5,
  },
  {
    text: "Great local spot with fair prices. The gyro plate is underrated and the coffee is always fresh. Perfect diner experience.",
    author: "Jennifer L.",
    rating: 5,
  },
];
const overallRating = 4.8;
const reviewCount = 127;
```

2. Build the social proof section:
   - Outer section: `py-16 md:py-20 px-4 md:px-8 bg-brand-accent/40 bg-texture-warm`
   - Inner container: `mx-auto max-w-5xl`

3. Section header (centered):
   - Accent bar: `w-12 h-1 bg-brand-primary mx-auto mb-4 rounded-full`
   - Heading: "What Our Customers Say" — font-heading, text-2xl md:text-3xl, font-bold, text-brand-text-heading, text-center, mb-2
   - Overall rating display centered below heading:
     - A row of 5 star SVGs (inline, w-5 h-5 each, fill brand-secondary):
       - Create a reusable star SVG: `<svg viewBox="0 0 20 20" fill="currentColor"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/></svg>`
     - All 5 stars filled in brand-secondary color
     - Next to stars: `{overallRating}` in font-bold text-brand-text-heading, then `"({reviewCount} reviews)"` in text-brand-text-muted text-sm
   - `mb-10` after the rating row

4. Reviews grid: `grid md:grid-cols-3 gap-6`
   - Each review card: `bg-white rounded-xl p-6 shadow-sm border border-brand-divider/50`
   - Inside each card:
     - Row of star SVGs for that review's rating (same star SVG, brand-secondary, w-4 h-4, mb-3)
     - Quote text: `text-brand-text-body text-sm md:text-base leading-relaxed mb-4` — wrap in quotation marks or use a subtle opening quote mark
     - Author: `text-brand-text-muted text-sm font-medium` — preceded by an em dash

5. Add a "View on Google" placeholder link centered below the cards:
   - `text-center mt-8`
   - Text: "See all reviews on Google" as a muted link
   - `text-brand-text-muted text-sm hover:text-brand-primary transition-colors`
   - For now, href="#" with a comment: `<!-- TODO: Replace with Google Business Profile URL -->`

CRITICAL constraints:
- The reviews are placeholder content — style them well but they'll be replaced with real Google reviews later
- Do NOT use any external review widget or API
- Stars must be inline SVGs, not an icon library
- Use warm background (brand-accent) to create visual distinction from white sections above
  </action>
  <verify>
    <automated>cd /Users/kybernet/Projects/mikesconey && test -f src/components/SocialProof.astro && grep -q "reviews" src/components/SocialProof.astro && grep -q "svg" src/components/SocialProof.astro && echo "PASS: SocialProof complete" || echo "FAIL"</automated>
  </verify>
  <done>SocialProof.astro exists with 3 placeholder customer reviews in a responsive grid, 5-star rating SVGs, overall rating display (4.8 stars, 127 reviews), warm accent background, and Google reviews placeholder link.</done>
</task>

</tasks>

<verification>
1. `npx astro check` — no type errors
2. `npx astro build` — builds successfully
3. AboutTeaser.astro has `/about/` link and uses site.ts data
4. SocialProof.astro has 3 review cards with star SVGs
5. Both components use brand color palette (not default Tailwind colors)
6. No external dependencies added
</verification>

<success_criteria>
- AboutTeaser.astro renders warm personality copy with "Read Our Story" link to /about/
- SocialProof.astro renders 3 placeholder reviews with star ratings on a warm background
- Both components use brand colors and feel distinctive (DSGN-02)
- No external image URLs or icon libraries
- Build passes
</success_criteria>

<output>
After completion, create `.canopy/phases/02-homepage/02-02-SUMMARY.md`
</output>
